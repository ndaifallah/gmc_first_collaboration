ALGORITHM three_add

VAR
    // Déclaration des variables (ingrédients)
    a, b, c: INTEGER;
    s: INTEGER;
    counter01: INTEGER;
    _counter: INTEGER;
    Counter: INTEGER;
    counter1: INTEGER;
    counter_1: INTEGER;
    hamid_khou_said: STRING;

BEGIN
    Read(a);
    Read(b);
    Read(c);
    s := a + b + c;
    Write(s);



END


